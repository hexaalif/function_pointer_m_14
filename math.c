#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    // double x, y;
    // scanf("%lf %lf", &x, &y);
    // int ans = ceil(x);
    // int ans = floor(x);
    // int ans = round(x);
    // int ans = sqrt(x);
    // double ans = pow(x * 1.0, y * 1.0);

    int x1;
    scanf("%d", &x1);
    int ans = abs(x1);
    printf("%d", ans);

    return 0;
}